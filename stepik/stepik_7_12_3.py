'''
Подвиг 3. Объявите функцию, которая принимает строку на кириллице и преобразовывает ее в латиницу,
используя следующий словарь для замены русских букв на соответствующее латинское написание:

t = {'ё': 'yo', 'а': 'a', 'б': 'b', 'в': 'v', 'г': 'g', 'д': 'd', 'е': 'e', 'ж': 'zh',
     'з': 'z', 'и': 'i', 'й': 'y', 'к': 'k', 'л': 'l', 'м': 'm', 'н': 'n', 'о': 'o', 'п': 'p',
     'р': 'r', 'с': 's', 'т': 't', 'у': 'u', 'ф': 'f', 'х': 'h', 'ц': 'c', 'ч': 'ch', 'ш': 'sh',
     'щ': 'shch', 'ъ': '', 'ы': 'y', 'ь': '', 'э': 'e', 'ю': 'yu', 'я': 'ya'}

Функция должна возвращать преобразованную строку. Замены делать без учета регистра (исходную строку
перевести в нижний регистр - малые буквы).

Определите декоратор с параметром chars и начальным значением " !?", который данные символы преобразует
в символ "-" и, кроме того, все подряд идущие дефисы (например, "--" или "---") приводит к одному дефису.
Полученный результат должен возвращаться в виде строки.

Примените декоратор с аргументом chars="?!:;,. " к функции и вызовите декорированную функцию для введенной строки s:
s = input()
Результат отобразите на экране.

Sample Input:  Декораторы - это круто!
Sample Output:  dekoratory-eto-kruto-
'''
t = {'ё': 'yo', 'а': 'a', 'б': 'b', 'в': 'v', 'г': 'g', 'д': 'd', 'е': 'e', 'ж': 'zh',
     'з': 'z', 'и': 'i', 'й': 'y', 'к': 'k', 'л': 'l', 'м': 'm', 'н': 'n', 'о': 'o', 'п': 'p',
     'р': 'r', 'с': 's', 'т': 't', 'у': 'u', 'ф': 'f', 'х': 'h', 'ц': 'c', 'ч': 'ch', 'ш': 'sh',
     'щ': 'shch', 'ъ': '', 'ы': 'y', 'ь': '', 'э': 'e', 'ю': 'yu', 'я': 'ya'}

# Функция для преобразования строки из кириллицы в латиницу


def latinize(s):
    # Приводим строку к нижнему регистру
    s = s.lower()
    # Инициализируем пустую строку для результата
    result = ''
    # Итерируем по символам строки
    for c in s:
        # Если символ есть в словаре t, добавляем его латинский аналог в результат
        if c in t:
            result += t[c]
        # Иначе добавляем символ как есть
        else:
            result += c
    # Возвращаем преобразованную строку
    return result
# Декоратор для замены символов на дефисы


def replace_chars(chars):
    # Объявляем сам декоратор
    def decorator(func):
        # Объявляем обертку
        def wrapper(s):
            # Вызываем функцию, переданную декоратору, чтобы получить результат
            s = func(s)
            # Заменяем указанные символы на дефисы
            for c in chars:
                s = s.replace(c, '-')
            # Сводим подряд идущие дефисы к одному
            while '--' in s:
                s = s.replace('--', '-')
            # Возвращаем измененную строку
            return s
        # Возвращаем обертку
        return wrapper
    # Возвращаем сам декоратор
    return decorator


# Создаем декорированную функцию decorated, которая будет сначала преобразовывать введенную строку в латиницу
# с помощью функции latinize, а затем заменять указанные символы на дефисы с помощью функции replace_chars.
decorated = replace_chars("?!:;,. ")(latinize)
# s = input()
s = 'Декораторы - это круто!'
result = decorated(s)
print(result)
